const BLOCK_SIZE_PX = 16
const GLOBALS = {
    CANVAS: {
        WIDTH: BLOCK_SIZE_PX * 20,
        HEIGHT: BLOCK_SIZE_PX * 20,
    },

    COLORS: {
        GRAY: "#404040",
        CYAN: "#42d8e3",
    },
}

export { BLOCK_SIZE_PX, GLOBALS }
